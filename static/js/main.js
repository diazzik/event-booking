document.addEventListener(
    "DOMContentLoaded",
    function () {

        console.log(
            "TicketBooking loaded"
        );

    }
);

function confirmDelete() {

    return confirm(
        "Удалить запись?"
    );

}

